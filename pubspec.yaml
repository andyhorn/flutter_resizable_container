name: flutter_resizable_container
description: Add resizable containers to your Flutter app.
version: 0.1.3
homepage: https://github.com/andyhorn/flutter_resizable_container

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
